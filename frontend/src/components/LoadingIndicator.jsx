import React from "react";

const LoadingIndicator = () => {
  return <div className="loading">Loading...(Fetching data takes about 1 minute when first started after long time)</div>;
};

export default LoadingIndicator;
